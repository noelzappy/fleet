# GENERATED by `fleet orchestrator init` — do not hand-edit.
# Publishes the UI and API on the Tailscale IP (in addition to the loopback binds in
# docker-compose.selfhost.yml). Docker publishes bypass ufw, so this must never be 0.0.0.0.
services:
  backend:
    ports:
      - "{{ .Bind }}:8080:8080"
  frontend:
    ports:
      - "{{ .Bind }}:3000:3000"
